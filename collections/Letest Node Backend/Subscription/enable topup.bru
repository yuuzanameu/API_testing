meta {
  name: enable topup
  type: http
  seq: 6
}

post {
  url: {{URL}}/api/v1/subscription/topup/enable_topup?plan_id=2
  body: none
  auth: bearer
}

params:query {
  plan_id: 2
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
