meta {
  name: Set Two factor
  type: http
  seq: 12
}

get {
  url: {{URL}}/api/v1/generate-2fa-secret
  body: text
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
