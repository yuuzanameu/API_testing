meta {
  name: reset password
  type: http
  seq: 9
}

post {
  url: {{URL}}/api/v1/reset-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      "new_password" : "Prateek@1991",
      "confirm_password" : "Prateek@1991",
      "token" : "{{vault:json-web-token}}"
  }
}

settings {
  encodeUrl: true
}
