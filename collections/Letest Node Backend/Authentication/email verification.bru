meta {
  name: email verification
  type: http
  seq: 7
}

post {
  url: {{URL}}/api/v1/verify-email
  body: json
  auth: inherit
}

body:json {
  {
      "token" : "{{vault:json-web-token}}"
  }
}

settings {
  encodeUrl: true
}
