meta {
  name: Get user's tickets
  type: http
  seq: 1
}

get {
  url: {{URL}}/api/v1/support-tickets
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
