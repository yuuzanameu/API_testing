meta {
  name: Get support ticket categories
  type: http
  seq: 2
}

get {
  url: {{URL}}/api/v1/support-ticket-categories
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
